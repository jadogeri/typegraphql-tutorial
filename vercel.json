{
  "version": 2,
  "builds": [
    {
      "src": "server/src/app.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["server/src/**"] }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "server/src/app.ts" }
  ]
}
